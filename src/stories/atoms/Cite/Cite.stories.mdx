import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { within } from '@storybook/testing-library'
import { expect } from '@storybook/jest'

import { Cite } from './Cite'

<Meta title="Atoms/Cite" component={Cite} />

# Cite

## A container used for citing the title of a creative work

The Cite component is used to mark up the title of a cited creative work. The
reference may be in an abbreviated form according to context-appropriate
conventions related to citation metadata.

<ArgsTable story="Default" />

export const Template = (args) => <Cite {...args} />

export const content = 'Example Cite Title'
export const Example = () => content

## Default

<Canvas>
  <Story
    name="Default"
    args={{
      children: Example()
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Controls

Experiment with this story with the Controls addon in the Canvas tab.

<Canvas>
  <Story
    name="controls"
    args={{
      isTight: true,
      children: Example()
    }}
    play={async ({ canvasElement }) => {
      const canvas = within(canvasElement)
      // 👇 Simulate interactions with the component
      await expect(canvas.getByText(content)).toBeInTheDocument()
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
