import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { within } from '@storybook/testing-library'
import { expect } from '@storybook/jest'

import { Box } from './Box'

<Meta title="Atoms/Box" component={Box} />

# Box

## A container used for spacing around elements

The Box component is used to add spacing (padding only) around elements.

<ArgsTable story="Default" />

export const Template = (args) => <Box {...args} />

export const content = 'Example box content'
export const Example = () => content

## Default

<Canvas>
  <Story
    name="Default"
    args={{
      children: Example()
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Tight

<Canvas>
  <Story
    name="Tight"
    args={{
      isTight: true,
      children: Example()
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Roomy

<Canvas>
  <Story
    name="Roomy"
    args={{
      isRoomy: true,
      children: Example()
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Controls

Experiment with this story with the Controls addon in the Canvas tab.

<Canvas>
  <Story
    name="controls"
    args={{
      isTight: true,
      children: Example()
    }}
    play={async ({ canvasElement }) => {
      const canvas = within(canvasElement)
      // 👇 Simulate interactions with the component
      await expect(canvas.getByText(content)).toBeInTheDocument()
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
